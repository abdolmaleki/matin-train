import 'package:flutter/material.dart';

class SectionModel{
  int number;
  String title;
  String description;
  Color color;

  SectionModel({required this.number, required this.title, required this.description,required this.color});
}